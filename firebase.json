{
    "firestore": [
        {
            "database": "(default)",
            "rules": "firestore.rules"
        },
        {
            "database": "adventure-streak-pre",
            "rules": "firestore.rules"
        }
    ]
}